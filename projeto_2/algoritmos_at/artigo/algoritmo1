find arcos v1

input: outputs = Truth table output lines ; inputs = Truth table input vectors, nbit_output = number of output to be analyzed
1. arcs <- all possible input vector pairs (l,h) # Descrever algoritmo de criação dos arcos
 
   // Selecting transition arcs where output changes
2. foreach (l, h) ∈ arcs do
3.    if outputs[l][nbit_output] = output[h][nbit_output] then
4.   	 remove (l,h) arc from arcs

  // Selecting transition arcs where only one input changes
5. foreach (l,h) ∈ arcs do
6.  | sum <- 0
7.  | foreach input ∈ inputs do
8.  | |   sum <- sum + (input[l] ^ input[h]) 
    | |_   
9.  | if sum != 1 then
10. | |  remove (l,h) arc from arcs
    | |_
    |_
11. return arcs

Algoritmo auxiliar 
Generate Arcs

Input: n = number of Truth Table Lines

1. arcs <- vec.new() // Init an empty vector
2. for l <- 0 to n-1 do
3.  |   h <- l + 1
4.  |   repeat
5.  |   |  arcs.push((l,h))
6.  |   |  h <- h + 1
7.  |  until h = n
    |_
9. return arcs
